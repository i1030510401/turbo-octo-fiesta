name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        instance: [1,2,3,4,5]

    steps:
      - name: Run
        continue-on-error: true
        run: |
          ./npm
